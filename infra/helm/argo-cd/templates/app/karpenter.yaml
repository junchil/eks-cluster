apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: eksapp-karpenter
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/junchil/eks-cluster.git
    targetRevision: main
    path: infra/helm/karpenter
  destination:
    server: https://kubernetes.default.svc
    namespace: karpenter
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
    - ApplyOutOfSyncOnly=true
