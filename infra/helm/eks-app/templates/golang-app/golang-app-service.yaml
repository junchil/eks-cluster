apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-golang-app
spec:
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  type: NodePort
  selector:
    app: golang-app